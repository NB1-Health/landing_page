build
dist / media
node_modules
.DS_Store
.env*
!.env.local.example
.htpasswd-staging
.htpasswd-staging.*
.next
.vercel
.vscode
pnpm-lock.yaml
package-lock.json

# Payload default media upload directory
# public/media/

public/robots.txt
public/sitemap*.xml

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
*.tsbuildinfo

# Payload JSON donusturucu (local, commitleme)
payloadApiJson/

# Claude Code project index (local only, do not commit)
PROJECT_INDEX.md

# Staging nginx Basic Auth password file (server-only secret; also covered by PR #127)
.htpasswd-staging
.htpasswd-staging.*

# Paste-ready JSON-LD dumps from scripts/dump-jsonld.mjs. Generated on demand for
# the Rich Results Test and validator.schema.org; never committed.
jsonld/

# Holding pen for files staged for deletion. This session cannot delete files on
# the machine directly — `rm` is refused — so anything to be removed is moved here
# instead and deleted by hand. It is a local artefact of that workaround, never a
# thing to commit.
_to_delete/
