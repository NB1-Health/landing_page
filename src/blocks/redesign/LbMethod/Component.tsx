'use client'

import React, { useState, useEffect } from 'react'
import RichText from '@/components/RichText'
import type { RdLbMethodBlock as Props } from '@/payload-types'

const mediaUrl = (m: unknown): string | undefined =>
  m && typeof m === 'object' && 'url' in m ? ((m as { url?: string }).url ?? undefined) : undefined

// GENERATED from manifests/section-04.json + bindings/RdLbMethod.json by
// tools/block_component.py — do not hand-edit; regenerate.
//
// Styles copied verbatim from the mockup; bindings replace content only.
// Layout and breakpoints come from rd-lb.css — this page's stylesheet,
// ported by port_css.py and rescoped from `.rd-block` to `.rd-lb` so it
// cannot reach any other page's blocks. That scope is why this root carries
// `rd-lb rd-lbmethod` and NOT `rd-block`.
//
// The two gut clouds are uploaded media, one per panel; the twelve circles that
// make the three tappable microbes are drawn inline so React owns the tap.


// GENERATED by tools/method_svg.py from the mockup's own circles — do not
// hand-edit; regenerate.
//
// The backdrop is an <image> pointing at the panel's uploaded figure. It is an
// <image> INSIDE the mockup's own <svg> rather than an <img> beside it, so the
// wrapper element, its viewBox and its aria-label stay exactly what the mockup
// wrote — and measured, it also rasterises closer: 0.79% of pixels differ at the
// circle edges against 12.75% for an <img>.
//
// x/y/width/height are the svg's OWN viewBox, not the image's. That is what lets
// an editor swap the file: the drawing is placed by the panel, so any SVG authored
// on the same 44.5 14.1 245.2 253.8 viewBox lands in register.
//
// No upload, no <image>. The svg still renders, so the three tappable microbes on
// the right-hand panel stay where they are rather than the whole figure vanishing.
//
// The twelve circles below are the three tappable stacks, copied verbatim. They
// are the LAST twelve in the mockup's own document order, which is what makes
// splitting them out safe: nothing is painted after them.
//
// They carry no data-n / data-j / data-s. In the mockup those attributes ARE
// the storage; here the three microbes are CMS fields, and a copy on the node
// would be a second place for the same string to live.
const RdLbMethodFigure: React.FC<{
  src?: string
  live?: boolean
  onPick?: (i: number) => void
}> = ({ src, live, onPick }) => (
  <>
    {src ? <image href={src} x="44.5" y="14.1" width="245.2" height="253.8" /> : null}
    {live ? (
      <>
      <circle cx="78.2" cy="112.9" r="8" fill="#F0F5FF" opacity="0.92" />
      <circle cx="78.2" cy="112.9" r="6.8" fill="rgba(81,71,69,.5)" opacity="0.18" />
      <circle className="halo" style={{ animationDelay: '1.35s' }} cx="78.2" cy="112.9" r="6.4" fill="rgba(81,71,69,.5)" />
      <circle className="node hero" cx="78.2" cy="112.9" r="5.6" fill="var(--nb1-lime)" stroke="#F0F5FF" strokeWidth="2" onClick={(e) => { e.stopPropagation(); onPick?.(0) }} />
      <circle cx="238.2" cy="140.6" r="8" fill="#F0F5FF" opacity="0.92" />
      <circle cx="238.2" cy="140.6" r="6.8" fill="var(--nb1-orange)" opacity="0.22" />
      <circle className="halo" style={{ animationDelay: '0.82s' }} cx="238.2" cy="140.6" r="6.4" fill="var(--nb1-orange)" />
      <circle className="node hero" cx="238.2" cy="140.6" r="5.6" fill="var(--nb1-orange)" stroke="#F0F5FF" strokeWidth="2" onClick={(e) => { e.stopPropagation(); onPick?.(1) }} />
      <circle cx="77.6" cy="167.6" r="8" fill="#F0F5FF" opacity="0.92" />
      <circle cx="77.6" cy="167.6" r="6.8" fill="rgba(81,71,69,.5)" opacity="0.22" />
      <circle className="halo" style={{ animationDelay: '0.82s' }} cx="77.6" cy="167.6" r="6.4" fill="rgba(81,71,69,.5)" />
      <circle className="node hero" cx="77.6" cy="167.6" r="6" fill="#F0F5FF" stroke="var(--nb1-lime)" strokeWidth="2" strokeDasharray="2.6 2" onClick={(e) => { e.stopPropagation(); onPick?.(2) }} />
      </>
    ) : null}
  </>
)

// Where each tappable microbe sits, in the figure's own viewBox units. The
// mockup reads this back off the DOM with getBoundingClientRect after the
// click; the numbers are known up front, so the popover lands right on the
// first paint instead of after a layout read.
const MIC_POS = [
  { cx: 78.2, cy: 112.9, r: 5.6 },
  { cx: 238.2, cy: 140.6, r: 5.6 },
  { cx: 77.6, cy: 167.6, r: 6 },
]
const MIC_BOX = { x: 44.5, y: 14.1, w: 245.2, h: 253.8 }

export const RdLbMethod: React.FC<Props> = ({ anchorId, footer, heading, hintActive, hintIdle, intro, left, leftFigure, microbes, reads, right, rightFigure, stateKey }) => {
  const [mic, setMic] = useState(null as number | null)

  // The popover's anchor. The mockup measures the tapped circle with
  // getBoundingClientRect and writes pixels; these are the same point expressed
  // as a percentage of the figure, which needs no layout read and so is correct
  // on the first paint rather than one frame later. `cy - r` is the TOP of the
  // circle, which is what the mockup used; the popover's own
  // `transform: translate(-50%, -118%)` lifts it from there.
  const micRow = mic === null ? null : (microbes || [])[mic]
  const micLeft =
    mic === null ? undefined
      : `${((MIC_POS[mic].cx - MIC_BOX.x) / MIC_BOX.w) * 100}%`
  const micTop =
    mic === null ? undefined
      : `${((MIC_POS[mic].cy - MIC_POS[mic].r - MIC_BOX.y) / MIC_BOX.h) * 100}%`

  // Dismiss on any click outside a microbe, the way the mockup does. The circles
  // stopPropagation, so a tap on one never reaches this.
  useEffect(() => {
    if (mic === null) return
    const hide = () => setMic(null)
    document.addEventListener('click', hide)
    return () => document.removeEventListener('click', hide)
  }, [mic])

  return (
      <section style={{
        background: "var(--nb1-cool-grey)"
      }} className="rd-lb rd-lbmethod" id={anchorId || undefined}>
        <div style={{
          maxWidth: "1240px",
          margin: "0px auto",
          padding: "64px 20px"
        }} data-d="pad bigpad">
          <div style={{
            fontFamily: "var(--nb1-font-primary)",
            fontWeight: "400",
            fontSize: "clamp(30px, 5cqi, 50px)",
            lineHeight: "1",
            letterSpacing: "-0.025em",
            maxWidth: "28ch"
          }} data-d="bigh">
            {(heading)?.root ? (
              <RichText data={heading} enableGutter={false} enableProse={false} />
            ) : null}
          </div>
          <p style={{
            fontFamily: "var(--nb1-font-secondary)",
            fontSize: "clamp(16px, 4.2cqi, 19px)",
            lineHeight: "1.5",
            opacity: "0.8",
            marginTop: "16px",
            maxWidth: "52ch"
          }}>{intro}</p>
          <div style={{
            display: "flex",
            flexDirection: "column",
            gap: "16px",
            marginTop: "34px"
          }} data-d="g2">
            <div style={{
              background: "var(--nb1-cool-grey)",
              boxShadow: "inset 0 0 0 1px var(--nb1-hairline)",
              borderRadius: "var(--nb1-radius-md)",
              padding: "26px 22px 28px",
              textAlign: "center"
            }}>
              <div style={{
                fontFamily: "var(--nb1-font-tertiary)",
                textTransform: "uppercase",
                letterSpacing: "0.12em",
                fontSize: "10.5px",
                color: "rgba(81, 71, 69, 0.8)"
              }}>{left?.eyebrow}</div>
              <div style={{
                fontFamily: "var(--nb1-font-primary)",
                fontSize: "clamp(22px, 4cqi, 28px)",
                lineHeight: "1.1",
                marginTop: "10px"
              }}>{left?.title}</div>
              <div style={{
                fontFamily: "var(--nb1-font-tertiary)",
                fontSize: "12px",
                color: "rgba(81, 71, 69, 0.86)",
                marginTop: "6px"
              }}>{left?.meta}</div>
              <div style={{
                maxWidth: "250px",
                margin: "14px auto 0px"
              }}>
                <svg style={{
                  width: "100%",
                  height: "auto",
                  display: "block",
                  overflow: "visible"
                }} viewBox="44.5 14.1 245.2 253.8" xmlns="http://www.w3.org/2000/svg" role="img" aria-label={left?.figureAlt || ''}>
                  <RdLbMethodFigure src={mediaUrl(leftFigure)} />
                </svg>
              </div>
              <p style={{
                fontFamily: "var(--nb1-font-secondary)",
                fontSize: "14px",
                lineHeight: "1.5",
                opacity: "0.72",
                marginTop: "16px"
              }}>{left?.caption}</p>
            </div>
            <div style={{
              background: "var(--nb1-cool-grey)",
              boxShadow: "inset 0 0 0 1px var(--nb1-hairline),0 30px 60px -40px rgba(81,71,69,.5)",
              borderRadius: "var(--nb1-radius-md)",
              padding: "26px 22px 28px",
              textAlign: "center"
            }}>
              <div style={{
                fontFamily: "var(--nb1-font-tertiary)",
                textTransform: "uppercase",
                letterSpacing: "0.12em",
                fontSize: "10.5px",
                color: "rgba(81, 71, 69, 0.8)"
              }} data-m="usfig">{right?.eyebrow}</div>
              <div style={{
                fontFamily: "var(--nb1-font-primary)",
                fontSize: "clamp(22px, 4cqi, 28px)",
                lineHeight: "1.1",
                marginTop: "10px"
              }}>{right?.title}</div>
              <div style={{
                fontFamily: "var(--nb1-font-tertiary)",
                fontSize: "12px",
                color: "rgba(81, 71, 69, 0.86)",
                marginTop: "6px"
              }}>{right?.meta}</div>
              <div style={{
                position: "relative",
                maxWidth: "250px",
                margin: "14px auto 0px"
              }}>
                <svg style={{
                  width: "100%",
                  height: "auto",
                  display: "block",
                  overflow: "visible"
                }} viewBox="44.5 14.1 245.2 253.8" xmlns="http://www.w3.org/2000/svg" role="img" aria-label={right?.figureAlt || ''}>
                  <RdLbMethodFigure src={mediaUrl(rightFigure)} live={true} onPick={setMic} />
                </svg>
                <div style={{ ...{
                  position: "absolute",
                  zIndex: "6",
                  width: "206px",
                  textAlign: "left",
                  background: "var(--nb1-dark-brown)",
                  color: "var(--nb1-cool-grey)",
                  borderRadius: "11px",
                  padding: "11px 13px",
                  boxShadow: "rgba(20, 16, 15, 0.55) 0px 16px 36px -12px",
                  transform: "translate(-50%, -118%)",
                  pointerEvents: "none"
                }, display: mic === null ? 'none' : 'block', left: micLeft, top: micTop }}>
                  <div style={{
                    fontFamily: "var(--nb1-font-tertiary)",
                    textTransform: "uppercase",
                    letterSpacing: "0.07em",
                    fontSize: "10.5px",
                    color: "var(--nb1-blue-grey)"
                  }}>{micRow?.status}</div>
                  <div style={{
                    fontFamily: "var(--nb1-font-primary)",
                    fontSize: "14px",
                    lineHeight: "1.2",
                    marginTop: "4px"
                  }}>{micRow?.name}</div>
                  <div style={{
                    fontFamily: "var(--nb1-font-secondary)",
                    fontSize: "12px",
                    lineHeight: "1.4",
                    color: "rgba(240, 245, 255, 0.84)",
                    marginTop: "3px"
                  }}>{micRow?.job}</div>
                  <span style={{
                    position: "absolute",
                    left: "50%",
                    top: "100%",
                    transform: "translateX(-50%)",
                    width: "0px",
                    height: "0px",
                    borderLeft: "7px solid transparent",
                    borderRight: "7px solid transparent",
                    borderTop: "7px solid var(--nb1-dark-brown)"
                  }} />
                </div>
              </div>
              <div style={{
                display: "flex",
                justifyContent: "center",
                marginTop: "16px"
              }}>
                <span style={{
                  display: "inline-flex",
                  alignItems: "center",
                  gap: "9px",
                  background: "var(--nb1-lime)",
                  color: "var(--nb1-black)",
                  borderRadius: "var(--nb1-radius-pill)",
                  padding: "0.6em 1.1em",
                  fontFamily: "var(--nb1-font-tertiary)",
                  textTransform: "uppercase",
                  letterSpacing: "0.06em",
                  fontSize: "10.5px"
                }}>
                  {mic === null ? hintIdle : hintActive}
                </span>
              </div>
              <div style={{
                display: "grid",
                gridTemplateColumns: "1fr 1fr",
                gap: "9px 18px",
                marginTop: "18px",
                textAlign: "left"
              }} data-m="fourup">
                {(reads || []).map((rd, rdIdx) => (
                  <div key={rdIdx} style={{
                    display: "flex",
                    alignItems: "baseline",
                    gap: "8px"
                  }}>
                    <span style={{
                      fontFamily: "var(--nb1-font-tertiary)",
                      fontSize: "10.5px",
                      color: "rgba(81, 71, 69, 0.6)"
                    }}>
                      {rd.number}
                    </span>
                    <span style={{
                      fontFamily: "var(--nb1-font-secondary)",
                      fontSize: "13.5px",
                      lineHeight: "1.4"
                    }}>
                      {rd.text}
                    </span>
                  </div>
                ))}
              </div>
              <div style={{
                display: "flex",
                flexWrap: "wrap",
                justifyContent: "center",
                gap: "10px 18px",
                marginTop: "18px",
                paddingTop: "16px",
                borderTop: "1px solid rgba(81, 71, 69, 0.16)"
              }}>
                <span style={{
                  display: "inline-flex",
                  alignItems: "center",
                  gap: "8px",
                  fontFamily: "var(--nb1-font-tertiary)",
                  textTransform: "uppercase",
                  letterSpacing: "0.08em",
                  fontSize: "10.5px",
                  color: "rgba(81, 71, 69, 0.86)"
                }}>
                  <span style={{
                    width: "11px",
                    height: "11px",
                    borderRadius: "50%",
                    background: "var(--nb1-lime)",
                    boxShadow: "0 0 0 2px var(--nb1-cool-grey)",
                    flex: "0 0 auto"
                  }} />
                  {stateKey?.[0]?.label}
                </span>
                <span style={{
                  display: "inline-flex",
                  alignItems: "center",
                  gap: "8px",
                  fontFamily: "var(--nb1-font-tertiary)",
                  textTransform: "uppercase",
                  letterSpacing: "0.08em",
                  fontSize: "10.5px",
                  color: "rgba(81, 71, 69, 0.86)"
                }}>
                  <span style={{
                    width: "11px",
                    height: "11px",
                    borderRadius: "50%",
                    background: "var(--nb1-orange)",
                    boxShadow: "0 0 0 2px var(--nb1-cool-grey)",
                    flex: "0 0 auto"
                  }} />
                  {stateKey?.[1]?.label}
                </span>
                <span style={{
                  display: "inline-flex",
                  alignItems: "center",
                  gap: "8px",
                  fontFamily: "var(--nb1-font-tertiary)",
                  textTransform: "uppercase",
                  letterSpacing: "0.08em",
                  fontSize: "10.5px",
                  color: "rgba(81, 71, 69, 0.86)"
                }}>
                  <span style={{
                    width: "11px",
                    height: "11px",
                    borderRadius: "50%",
                    background: "var(--nb1-cool-grey)",
                    border: "1.5px dashed var(--nb1-lime)",
                    flex: "0 0 auto"
                  }} />
                  {stateKey?.[2]?.label}
                </span>
              </div>
            </div>
          </div>
          <div style={{
            display: "flex",
            flexDirection: "column",
            gap: "16px",
            marginTop: "22px"
          }} data-d="g2">
            <div style={{
              textAlign: "center",
              padding: "0px 8px"
            }}>
              <div style={{
                fontFamily: "var(--nb1-font-tertiary)",
                textTransform: "uppercase",
                letterSpacing: "0.1em",
                fontSize: "10.5px",
                color: "rgba(81, 71, 69, 0.8)"
              }}>{footer?.leftEyebrow}</div>
              <p style={{
                fontFamily: "var(--nb1-font-secondary)",
                fontSize: "16px",
                lineHeight: "1.45",
                marginTop: "9px"
              }}>{footer?.leftText}</p>
            </div>
            <div style={{
              textAlign: "center",
              padding: "0px 8px"
            }}>
              <div style={{
                fontFamily: "var(--nb1-font-tertiary)",
                textTransform: "uppercase",
                letterSpacing: "0.1em",
                fontSize: "10.5px",
                color: "rgba(81, 71, 69, 0.8)"
              }}>{footer?.rightEyebrow}</div>
              <p style={{
                fontFamily: "var(--nb1-font-secondary)",
                fontSize: "16px",
                lineHeight: "1.45",
                marginTop: "9px"
              }}>
                <b style={{
                  fontWeight: "500"
                }}>{footer?.rightText}</b>
              </p>
            </div>
          </div>
        </div>
      </section>
  )
}

// RenderBlocks.client.tsx imports every block as `<Name>Component`; the alias
// keeps that one import name stable whatever the component itself is called.
export const RdLbMethodComponent = RdLbMethod

export default RdLbMethod
